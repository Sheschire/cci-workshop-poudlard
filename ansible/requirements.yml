---
# Galaxy collections required by the playbooks.
#   ansible-galaxy collection install -r ansible/requirements.yml
collections:
  # docker_swarm, docker_swarm_info, docker_node, docker_network
  - name: community.docker
    version: ">=4.1.0,<5.0.0"
  # ufw-free iptables helpers, timezone, ...
  - name: community.general
    version: ">=10.0.0,<12.0.0"
  # sysctl, mount, authorized_key helpers
  - name: ansible.posix
    version: ">=1.6.0,<3.0.0"
